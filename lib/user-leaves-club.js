class UserLeavesClub {
  constructor({ userId, clubId, database }) {
    this.userId = userId;
    this.clubId = clubId;;
    this.database = database;
  }

  execute() {
  }
}

module.exports = UserLeavesClub;
