class UserSearchesClub {
  constructor({ userId, clubName, database }) {
    this.userId = userId;
    this.clubName = clubName;
    this.database = database;
  }

  execute() {
  }
}

module.exports = UserSearchesClub;
